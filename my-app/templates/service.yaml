apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-app.fullname" . }}
spec:
  selector:
    app: {{ include "my-app.name" . }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
